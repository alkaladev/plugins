<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autorol - Role Assignment</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: white;
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            color: #667eea;
            margin-bottom: 10px;
        }

        .header p {
            color: #666;
        }

        .content {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .section-title {
            font-size: 24px;
            color: #667eea;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #667eea;
        }

        .info-box {
            background: #f0f4ff;
            border-left: 4px solid #667eea;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        .info-box p {
            color: #555;
            line-height: 1.6;
        }

        .messages-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .message-card {
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 15px;
            background: #fafafa;
            transition: all 0.3s ease;
        }

        .message-card:hover {
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.2);
            border-color: #667eea;
        }

        .message-title {
            font-size: 18px;
            font-weight: bold;
            color: #667eea;
            margin-bottom: 10px;
        }

        .message-desc {
            color: #666;
            font-size: 14px;
            margin-bottom: 10px;
        }

        .message-buttons-count {
            color: #999;
            font-size: 12px;
            margin-bottom: 15px;
        }

        .buttons-group {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: #667eea;
            color: white;
        }

        .btn-primary:hover {
            background: #5568d3;
            transform: translateY(-2px);
        }

        .btn-danger {
            background: #ff6b6b;
            color: white;
        }

        .btn-danger:hover {
            background: #ee5a52;
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: #95a5a6;
            color: white;
        }

        .btn-secondary:hover {
            background: #7f8c8d;
        }

        .empty-state {
            text-align: center;
            padding: 40px;
            color: #999;
        }

        .empty-state-icon {
            font-size: 48px;
            margin-bottom: 15px;
            opacity: 0.5;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #333;
            font-weight: 600;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-family: inherit;
        }

        .form-group textarea {
            resize: vertical;
            min-height: 80px;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .alert {
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .alert-info {
            background: #e3f2fd;
            color: #1976d2;
            border-left: 4px solid #1976d2;
        }

        .alert-success {
            background: #e8f5e9;
            color: #388e3c;
            border-left: 4px solid #388e3c;
        }

        .alert-warning {
            background: #fff3e0;
            color: #f57c00;
            border-left: 4px solid #f57c00;
        }

        .alert-danger {
            background: #ffebee;
            color: #d32f2f;
            border-left: 4px solid #d32f2f;
        }

        @media (max-width: 768px) {
            .messages-grid {
                grid-template-columns: 1fr;
            }

            .header,
            .content {
                padding: 20px;
            }

            .section-title {
                font-size: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>👑 Autorol - Asignación de Roles</h1>
            <p>Crea embeds con botones para que los usuarios obtengan roles automáticamente</p>
        </div>

        <div class="content">
            <div class="section-title">📋 Sistemas de Roles</div>

            <div class="info-box">
                <p>
                    <strong>💡 Tip:</strong> 
                    Usa los comandos en Discord para crear y gestionar tus sistemas de roles. 
                    Este dashboard te permite ver y eliminar sistemas existentes.
                </p>
            </div>

            <div id="messages-container" class="messages-grid">
                <div class="empty-state">
                    <div class="empty-state-icon">📭</div>
                    <p>No hay sistemas de roles configurados aún.</p>
                    <p style="font-size: 12px; margin-top: 10px; color: #bbb;">
                        Usa <code style="background: #f0f0f0; padding: 2px 6px; border-radius: 3px;">/asetup</code> 
                        en Discord para crear tu primer sistema.
                    </p>
                </div>
            </div>

            <div style="margin-top: 30px; padding-top: 30px; border-top: 1px solid #eee;">
                <h3 style="color: #667eea; margin-bottom: 15px;">📖 Comandos Disponibles</h3>
                <div style="background: #f9f9f9; padding: 15px; border-radius: 5px; font-size: 13px; line-height: 1.8; color: #555;">
                    <div><strong>/asetup</strong> - Crear un nuevo embed de roles</div>
                    <div><strong>/anadir</strong> - Añadir un botón de rol</div>
                    <div><strong>/remover</strong> - Remover un botón</div>
                    <div><strong>/aeditar</strong> - Editar un embed</div>
                    <div><strong>/alista</strong> - Ver todos los sistemas</div>
                    <div><strong>/aeliminar</strong> - Eliminar un sistema completo</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Cargar mensajes
        async function loadMessages() {
            try {
                const response = await fetch('./api/messages');
                const messages = await response.json();
                
                const container = document.getElementById('messages-container');
                
                if (!messages || messages.length === 0) {
                    container.innerHTML = `
                        <div class="empty-state">
                            <div class="empty-state-icon">📭</div>
                            <p>No hay sistemas de roles configurados aún.</p>
                            <p style="font-size: 12px; margin-top: 10px; color: #bbb;">
                                Usa <code style="background: #f0f0f0; padding: 2px 6px; border-radius: 3px;">/asetup</code> 
                                en Discord para crear tu primer sistema.
                            </p>
                        </div>
                    `;
                    return;
                }

                container.innerHTML = messages.map(msg => `
                    <div class="message-card">
                        <div class="message-title">${msg.title}</div>
                        <div class="message-desc">${msg.description}</div>
                        <div class="message-buttons-count">
                            Botones: <strong>${msg.buttons ? msg.buttons.length : 0}/25</strong>
                        </div>
                        <div class="buttons-group">
                            <button class="btn btn-danger" onclick="deleteMessage('${msg.messageId}')">
                                Eliminar
                            </button>
                        </div>
                    </div>
                `).join('');
            } catch (error) {
                console.error('Error cargando mensajes:', error);
            }
        }

        // Eliminar mensaje
        async function deleteMessage(messageId) {
            if (!confirm('¿Estás seguro de que quieres eliminar este sistema de roles?')) {
                return;
            }

            try {
                const response = await fetch(`./api/messages/${messageId}`, {
                    method: 'DELETE'
                });

                if (response.ok) {
                    alert('✅ Sistema eliminado correctamente');
                    loadMessages();
                } else {
                    alert('❌ Error al eliminar el sistema');
                }
            } catch (error) {
                console.error('Error eliminando mensaje:', error);
                alert('❌ Error al eliminar el sistema');
            }
        }

        // Cargar mensajes al iniciar
        loadMessages();

        // Recargar cada 5 segundos
        setInterval(loadMessages, 5000);
    </script>
</body>
</html>
